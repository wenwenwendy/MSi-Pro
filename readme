##environment requirments##
python==3.8.5 
tensorflow ==2.2
keras==2.4.3

##usage##
#train:
class_Model.py is the main function, which comtains a class called ModelDemo. There's a train function in the class and 
using the def train() can train the data you input and return a model. 
#pridict:
using the def predict() in ModelDemo class can geiv the prediction that a peptide belonging to one allel and one length
